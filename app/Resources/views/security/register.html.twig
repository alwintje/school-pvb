{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('bundles/AppBundle/css/register.css') }}" />
{% endblock %}

{% block body %}
<div class="register">
    <div class="heading">
        <h2>Registreer</h2>
        <span class="response">{{ response }}</span>
        {{ form_start(form, {'attr':{'class': 'form-group'} } ) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
        {#input-group input-group-lg#}
    </div>
</div>
{% endblock %}
